using System;

namespace ClashRoyaleProxy
{
    /// <summary>
    /// Specifies if a packet is client or server sided
    /// </summary>
    enum DataDestination
    {
        FROM_CLIENT,
        FROM_SERVER
    }
}
